spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orion_products
    username: gabdev
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true